% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/diagnostics.R
\name{fat_validate}
\alias{fat_validate}
\title{Validate internal consistency of FAT/DFAT outputs}
\usage{
fat_validate(
  predictions,
  results,
  data,
  unit_var,
  time_var,
  outcome_var,
  forecast_lag = 0,
  dfat_mode = FALSE,
  control_group_value = NULL
)
}
\arguments{
\item{predictions}{The \code{results$predictions} data.frame from \code{estimate_fat()}.}

\item{results}{The \code{results$results} data.frame from \code{estimate_fat()}.}

\item{data}{The original long panel used as input to \code{estimate_fat()}.}

\item{unit_var}{Unit id column (string).}

\item{time_var}{Time column (string).}

\item{outcome_var}{Outcome column (string).}

\item{forecast_lag}{Integer; same lag you passed to \code{estimate_fat()}.}

\item{dfat_mode}{Logical; TRUE if you ran DFAT (i.e., you had a \code{treated} column and set \code{control_group_value}).}

\item{control_group_value}{Value used to mark the control group in \code{treated} (e.g., FALSE or 0). Required if \code{dfat_mode=TRUE}.}
}
\value{
tibble with columns: check, passed (logical), details (character)
}
\description{
Runs a suite of invariants on the outputs of \code{estimate_fat()} and returns
a tibble of checks with pass/fail and short details.
}
